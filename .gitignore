# Node deps
node_modules/

# Local env files (do NOT commit real secrets)
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime persisted data (may contain user IDs or secrets)
allowed_users.txt

# OS files
.DS_Store
